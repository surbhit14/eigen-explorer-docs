---
openapi: get /metrics/tvl
---

<Note>
    The total TVL ("tvl"), normalized in `ETH`, does not include the TVL of
    `Eigen` tokens as they are currently not transferable and therefore do not
    have an exchange rate with `ETH`.
</Note>
