---
openapi: get /metrics/tvl/restaking
---

<Note>
    The total TVL ("tvl"), normalized in `ETH`, do not include the TVL of
    `Eigen` tokens as they are currently not transferable. `Eigen` TVL is
    denominated in `Eigen` tokens.
</Note>
