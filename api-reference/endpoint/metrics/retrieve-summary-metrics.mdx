---
openapi: get /metrics
---

<Note>
    The total TVL ("tvl") and TVL Restaking ("tvlRestaking"), normalized in
    `ETH`, do not include the TVL of `Eigen` tokens as they are currently not
    transferable. `Eigen` TVL is denominated in `Eigen` tokens.
</Note>
